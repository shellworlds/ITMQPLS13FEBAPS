apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantum-autonomous-formulation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantum
  template:
    metadata:
      labels:
        app: quantum
    spec:
      containers:
      - name: solver
        image: quantum/autonomous-formulation:latest
