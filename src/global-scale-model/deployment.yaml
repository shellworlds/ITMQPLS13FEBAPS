apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantum-global-scale-model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantum
  template:
    metadata:
      labels:
        app: quantum
    spec:
      containers:
      - name: solver
        image: quantum/global-scale-model:latest
