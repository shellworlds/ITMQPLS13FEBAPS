apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantum-grid-optimization-qaoa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantum
  template:
    metadata:
      labels:
        app: quantum
    spec:
      containers:
      - name: solver
        image: quantum/grid-optimization-qaoa:latest
