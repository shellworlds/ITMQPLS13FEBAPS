apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantum-site-selection-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantum
  template:
    metadata:
      labels:
        app: quantum
    spec:
      containers:
      - name: solver
        image: quantum/site-selection-ai:latest
